server {
    listen 80;
    
    location /appmonitoreo/ {
        alias /usr/share/nginx/html/;
        try_files $uri $uri/ /appmonitoreo/index.html;
    }

    # Root redirect to subpath for convenience
    location = / {
        return 301 /appmonitoreo/;
    }
}
